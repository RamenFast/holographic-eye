[package]
name = "holographic-eye"
version = "1.0.3"
description = "The Holographic Eye — glass cockpit for the Hermes holographic memory"
authors = ["Ben"]
license = "GPL-3.0-only"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
dirs = "5"

[profile.release]
strip = true
lto = true
codegen-units = 1
